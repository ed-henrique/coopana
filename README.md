# COOPANA

## How to run

### Backend

```bash
cd backend
npm install
touch .env
```

- Follow `.env.example` template, adding the values to the fields:

```env
DB_NAME=
API_PORT=
```

- Run:

`npm start`

### Frontend

```bash
cd frontend
npm install
```

- Run:

`npm start`
